---
name: takeshiemoto:review
description: コード差分をレビュー指針に基づいてレビューする。「レビューして」「review」等の依頼時、または /takeshiemoto:review で呼び出す。
user-invocable: true
allowed-tools: Bash, Agent
---

# review

ベースブランチとの差分を観点別のサブエージェントで並列レビューし、オーケストレーターが全指摘の妥当性を網羅的にチェックした上で最終報告する。

## 使用方法

```sh
/takeshiemoto:review --base <ベースブランチ>
```

`--base` は必須。指定がなければ使い方を案内して即時終了する。

## 基本姿勢

- 根本を疑え。表面的な修正ではなく、そもそもこのアプローチでよいのかを問う
- 既存のコードは悪いコードであり、レビュー対象のコードを信頼せず、設計意図から検証する
- 美しいコード = 推測を要求しないコード
- state が型で見え、render は純粋で、effect は外部同期だけに使われるコードを基準とする

## 実行フロー

### Step 1: 差分収集

以下を実行し、結果を保持する。

```bash
git diff <base>...HEAD --name-only
git diff <base>...HEAD
```

直近コミットのみとの比較は禁止。必ず `<base>...HEAD` を使う。

### Step 2: 変更サマリー（非エンジニア向け）

Step 1 で取得した差分を基に、非エンジニアでも理解できる自然言語の説明文を以下のフォーマットで出力する。技術用語は避け、ユーザー体験や業務への影響を中心に記述する。

```text
## 変更サマリー

### 変更の概要
<この変更が何をするものか、1〜2文で端的に説明>

### 変更の目的
<なぜこの変更が必要なのか、背景や動機を説明>

### 影響範囲
- <変更によって影響を受ける画面・機能・ユーザー操作を箇条書き>

### 変更の内訳
| 区分 | 内容 |
| --- | --- |
| <新機能 / 修正 / 改善 / 削除> | <それぞれ何が変わったか平易に説明> |
```

#### 記述ルール

- コード片やファイルパスは含めない
- 「コンポーネント」「リファクタリング」「型」等の技術用語を使わず、ユーザーが触れる機能や画面の言葉で説明する
- 推測で目的を補わない。差分から読み取れる事実のみを記述する

### Step 3: 並列レビュー（一次レビュー）

以下の4つのエージェントを **単一メッセージで同時に** Agent ツールで起動する。全エージェント `model: "opus"` を指定する。

| name | subagent_type | model | color |
| --- | --- | --- | --- |
| `purity-reviewer` | `takeshiemoto:purity-reviewer` | `opus` | `red` |
| `effect-boundary-reviewer` | `takeshiemoto:effect-boundary-reviewer` | `opus` | `yellow` |
| `type-constraint-reviewer` | `takeshiemoto:type-constraint-reviewer` | `opus` | `purple` |
| `scope-creep-reviewer` | `takeshiemoto:scope-creep-reviewer` | `opus` | `orange` |

各 Agent 呼び出しの `prompt` には以下をそのまま含める:

```text
以下の差分をレビューしてください。

## 変更ファイル一覧
<Step 1 の --name-only 結果>

## 差分
<Step 1 の git diff 結果>
```

### Step 4: 妥当性チェック（オーケストレーター）

Step 3 の全エージェントの指摘を1件ずつ網羅的にチェックする。サブエージェントは使わず、オーケストレーター自身が差分と照合して判定する。

#### 4-1. 全指摘の列挙

Step 3 の結果から、全レビュアーの全指摘をエージェント名・ファイル・行番号付きで1つのリストに展開する。指摘ゼロのエージェントは除外する。

#### 4-2. 1件ずつの妥当性判定

列挙した各指摘について、以下の観点で **すべて** チェックし、判定結果を記録する。飛ばしや見落としを禁止する。

| チェック項目 | 除外条件 |
| --- | --- |
| 差分整合性 | 指摘対象の行が `<base>...HEAD` の差分に含まれない |
| 事実整合性 | 差分を再読した結果、指摘の前提（コードの挙動・型・依存）が誤っている |
| 根拠の妥当性 | 指摘の根拠がレビュー観点の具体的な条項に紐づかない |
| 差分スコープ | 差分と無関係な既存コードの改善提案になっている |
| 重複 | 同一箇所・同種の指摘が他のエージェントから出ている（最も具体的な1件のみ残す） |
| 過剰性 | 好みレベルで動作・保守性に実質的な影響がない |

いずれかに該当する指摘は除外する。除外した指摘は、除外理由とともに内部的に記録しておく（最終報告には出さない）。

#### 4-3. 深刻度の付与

残った指摘に深刻度を付与する:

- **must**: 修正しなければマージすべきでない（型安全性の欠損、責務の重大な混在、禁止パターンへの抵触）
- **should**: 修正を強く推奨する（設計改善、可読性の大幅な向上）
- **nit**: 軽微な改善提案

### Step 5: 最終報告

集約結果をファイルパス順に以下のフォーマットで報告する。

```text
## レビュー結果

### <ファイルパス>

**[must]** L<行番号>: <1行での指摘サマリー>
意図: <おそらくxxxxを実装しようとした>
手段: <そしてxxxxというアプローチをとった>
問題: <これはxxxx的にNGであり、根拠は「レビュー観点 > 条項名」>
正解: <xxxxすべきである>

<修正案があればコードブロックで提示>

**[should]** L<行番号>: <1行での指摘サマリー>
意図: <おそらくxxxxを実装しようとした>
手段: <そしてxxxxというアプローチをとった>
問題: <これはxxxx的にNGであり、根拠は「レビュー観点 > 条項名」>
正解: <xxxxすべきである>

<修正案があればコードブロックで提示>

**[nit]** L<行番号>: <1行での指摘サマリー>
意図: <おそらくxxxxを実装しようとした>
手段: <そしてxxxxというアプローチをとった>
問題: <これはxxxx的にNGであり、根拠は「レビュー観点 > 条項名」>
正解: <xxxxすべきである>
```

全エージェントが指摘ゼロ、または集約後に有効な指摘が残らなかった場合は「指摘なし」とだけ報告する。
